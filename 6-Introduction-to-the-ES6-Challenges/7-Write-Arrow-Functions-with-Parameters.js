// You can pass more than one argument into arrow function as well.
const myConcat = (arr1, arr2) => arr1.concat(arr2); // arrow function syntax

// test your code
console.log(myConcat([1, 2], [3, 4, 5]));
