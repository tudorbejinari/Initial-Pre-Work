// parseInt is a function that allows to parse a string and returns an integer.
// Very important to remember if the first character in the strings can't be converted into a number , then it returns NaN.
// Use parseInt() in the convertToInteger function so it converts the input string str into an integer, and returns it.
function convertToInteger(str) {
  return parseInt(str);
}

convertToInteger("56");
