// loops allows to run the same code moltiple times. "While" loop  it runs "while" a specified  condition is true and stops if its not true.
//
// Setup
var myArray = [];
var i = 0;
while(i < 5) {
  // while loop push values to an array from 0 to 4.
  myArray.push(i);
  i++;
}
// Only change code below this line.
