//  instead of having an if/else statement to do a comparasion we can just have a single line since  a < b   return true or false anyway.
function isLess(a, b) {
  // Fix this code
return a < b;
}

// Change these values to test
isLess(10, 15);
