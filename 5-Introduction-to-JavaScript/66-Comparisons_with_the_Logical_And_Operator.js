// Using logical and operator && you can test more then one thing at a time. returns true only when operants to the left and right are true.
function testLogicalAnd(val) {
  // Only change code below this line


    if (val <= 50 && val >= 25) {
      return "Yes";
    }


  // Only change code above this line
  return "No";
}

// Change this value to test
testLogicalAnd(25);
