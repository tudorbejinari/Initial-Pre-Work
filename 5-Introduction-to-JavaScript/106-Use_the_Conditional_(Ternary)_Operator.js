// in JavaSript there are different ways to simplify the code .
// For example this syntax ----   condition ? statement-if-true : statement-if-false; allows to to have only one line if-else expression.
function checkEqual(a, b) {
  return a == b ? true : false;
}

checkEqual(1, 2);
